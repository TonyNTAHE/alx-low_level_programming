#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first number.
 * @b: second number.
 * Return: multiplication of two numbers.
 */
int mul(int a, int b)
{
	int i;

	i = a * b;
	return (i);
}
